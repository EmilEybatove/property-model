#pragma once

#include <stdio.h>

#pragma once

namespace PropertyModel {
namespace Library {

template<size_t Index>
struct Data;

template<size_t Index>
struct Value;

template<size_t Index>
struct Output;
}; // namespace Library
}; // namespace PropertyModel
